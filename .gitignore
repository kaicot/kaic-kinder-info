# ── 비밀정보 (절대 커밋 금지) ────────────────────────────────
.env
.env.local
config.json
config.json.bak
# 작업 인계 문서 — 개인 맥락(아이 생년월·지역·로컬 경로)이 들어 있음
handoff.md

# ── 개인 환경에서 생성되는 데이터 ────────────────────────────
# 저장한 후보 목록. 후보 자체가 거주지·선호를 드러내는 개인정보.
shortlist.json
# API 응답 캐시. 재생성 가능하고 유치원 관계자 실명이 포함되어 있음.
cache/
# 사용자가 조회한 지역의 시군구 코드. 사용 지역이 드러나므로 제외.
sgg_codes.json

# ── 파이썬 ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 편집기 / OS ──────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
