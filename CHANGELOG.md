# 변경 이력

이 프로젝트는 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따릅니다.

## [1.10.0] - 2026-09-02

**유치원 자체 공시를 넘어 병설 모초교·자차 경로 안전·통학버스 등록을 출처별로 교차한다.**

### Added

- `schoolinfo.py`와 `mother-school` — 2026년 신규 키의 시도·시군구 필수 조건과
  `pbanYr`를 자동 처리하고 병설유치원의 모초등학교 급식·보건·시설안전을 보조로 표시한다.
- `traffic_safety.py`와 `traffic` — 도로교통공단 공식 CSV 2종을 30일마다 자동 갱신하고,
  OSRM 전체 자차 경로 GeoJSON과 공식 사고다발지 폴리곤을 교차한다. 직선 반경은
  접근성·안전 판단에 사용하지 않는다.
- `schoolbus.py`와 `bus` — 학교안전지원시스템 공개 통학버스 상세를 7일 캐시하고
  유치원알리미 차량 수와 교차확인한다. 차량번호와 개인 이름은 표시하지 않는다.
- `bus-import`·`bus-status` — 자동 조회 장애 때 사용자가 내려받은 XLSX를 비상 자료로
  가져오고 가져온 시각·해시·행 수를 기록한다.
- `health`·`finance` — 유치원알리미 웹 공시의 식중독·공기질·소독·음용수·미세먼지·
  조도와 예산·결산 추이를 키 없이 조회한다.
- `sources`, `refresh --source traffic|bus|schoolinfo`, `refresh --all`.
- `profile --extended`·`report --extended` 및 MCP 확장 도구 5종(총 18종).

### Changed

- `route.py`가 OSRM 전체 GeoJSON 경로를 별도 영구 캐시하며 교통안전 분석에 제공한다.
- 방문·전화 질문은 계속 기본 출력하지 않고 `report --questions`를 명시할 때만 생성한다.
- 웹 공시, 학교알리미, 교통공단, 학교안전지원시스템 중 한 출처가 실패해도 나머지는 유지한다.

### Fixed

- 사립과 다른 공립 병설의 예산·결산 차트 형식을 별도로 검산해 두 유형 모두 추이를 읽는다.
- 1분 미만의 짧은 자차 경로가 비교표에서 `0분`으로 반올림되지 않게 소수점으로 표시한다.

### Notes

- 교통안전 결과 0건은 안전 판정이 아니라 공식 사고다발지 선정 기준에 해당하는 구간이
  확인되지 않았다는 뜻이다. 실시간 교통은 반영하지 않는다.
- 학교알리미 결과는 병설유치원 자체 정보가 아니라 모초등학교 공시다.
- 어린이집 확장은 이번 버전의 범위에서 제외했다.

## [1.9.0] - 2026-09-02

**공시의 외곽 운영시간과 아이가 실제로 이용하는 시간을 분리했다.**

### Added

- **`hours` 명령** — 후보 1~6곳의 정규 교육과정·일반 방과후·조기돌봄·저녁돌봄·
  방학 중 운영과 이용 조건을 표로 비교한다. `--year`와 `--target`을 지원한다.
- `kinderweb.get_hours()` — 키 없이 웹 공시의 교육과정 시간, 전체 운영 외곽 범위,
  공식 교육계획서 첨부 정보를 라벨 기반으로 파싱하고 자체 검산한다.
- `verified_hours.py` — 공식 계획서에서 사람이 확인한 세부 운영시간을 학년도별로 검산·
  저장하는 로컬 계층. 실제 `verified_hours.json`은 후보·지역 정보 보호를 위해 gitignore 한다.
- MCP `kindergarten_hours` 추가 — MCP 도구 총 13종.

### Changed

- `profile --web`·`compare`·`report`가 `공시 전체 운영범위`와 정규·방과후·조기·저녁
  시간을 분리한다. 이전 학년도 계획서를 쓸 때는 요청 학년도의 **참고용**임을 표시한다.
- 로컬 공식문서 검증값과 웹 공시값이 충돌하면 하나를 조용히 덮지 않고 경고한다.
- `report`의 방문·전화 질문 목록은 기본 출력에서 제거하고, 사용자가 원할 때만
  `--questions`로 추가한다. 통학차량도 자동 문의 문구 대신 `노선 정보 미공시`로 표시한다.

### Notes

- `-`는 운영하지 않는다는 뜻이 아니라 현재 확보한 공식 자료에서 세부 시간을 분리하지
  못했다는 뜻이다. 다음 학년도 운영계획은 공개되기 전까지 확정값으로 표시하지 않는다.

## [1.8.0] - 2026-09-01

**혼합반 사각지대 수정.** Open API가 전용반과 통합 혼합반 수만 제공해,
혼합반으로 해당 연령을 받는 유치원을 `search --age`가 조용히 누락하던 결함을 바로잡았다.

### Fixed

- `search --age 3|4|5`가 전용반뿐 아니라 웹 공시의 `만3~4세`·`만4~5세`·`만3~5세`
  구성을 확인해 해당 연령이 포함된 혼합반도 후보에 넣는다. 웹 확인 실패 시 후보를
  삭제하지 않고 `혼합반 구성 미확인 ⚠`으로 남긴다.
- 검증 지역의 만3세 검색 실측에서 기존 15곳이 **25곳**으로 복구됐다. 전용 만3세반이 0이던
  한 유치원도 실제로는 만3~4세 2학급 + 만3~5세 1학급으로 만3세를 받고 있었다.
- `report` 방문 질문이 현재 혼합반 구성을 아는 경우 더 이상 “만3세를 모집하나요?”라고
  잘못 묻지 않고, 실제 구성과 합산 정원을 인용해 모집 인원·담임 배치를 묻는다.
- `trend`·`diff`에 **혼합반 원아**를 별도 열로 추가했다. `만3세 전용반 원아 0명`만으로
  만3세 미운영을 단정하지 않도록 안내한다.

### Added

- `kinderweb.get_age_classes()` — 현재 연령별 학급표를 라벨 기반으로 파싱하고
  전용반·3~4세·4~5세·3~5세별 학급/현원/정원을 반환한다. `selftest`는 4종으로 확장.
- 웹 공시 원문 7일 디스크 캐시(`cache/web/`). `refresh`가 API 캐시와 함께 비운다.
- `profile --web`·`report`에 현재 연령별 학급 구성표, report 비교표에
  `만N세 포함 혼합반` 행을 추가했다.
- `search --no-web` — 혼합반 세부 연령 확인을 생략하는 빠른 모드. 혼합반 후보는
  누락하지 않고 모두 미확인으로 표시한다.

### Notes

- 혼합반 정원·현원은 해당 연령만의 숫자가 아니라 **포함 연령 전체의 합계**다.
  실제 연령별 모집 인원은 유치원에 직접 확인해야 한다.

## [1.7.0] - 2026-09-01

**직선거리 대신 실제 자차 도로 거리.** 실측하니 우회율이 1.0~3.1배까지 벌어졌다 —
직선이 똑같이 541m인 두 유치원이 도로로는 720m와 1.20km였다.

### Added

- **`search --road`** — 자차 도로 거리·시간을 조회해 표에 `자차(도로)` 열을 붙인다.
  `--near` 와 함께 쓰면 도로 거리 기준으로 거르고 정렬한다. 도로 조회는 비싸므로
  직선거리로 넉넉히(지정 반경의 2배) 거른 뒤 남은 곳만 부른다 — 직선으로는 멀어 보여도
  도로로 가까운 곳을 놓치지 않기 위함이다.
- **`home` 명령** — 네이버지도 [공유] 링크를 넘기면 좌표를 뽑아 `.env`에 저장한다.
  `.env`를 직접 열 필요가 없다. 카카오맵 링크는 자체 좌표계(urlX/urlY)라 변환이
  부정확해 받지 않고, 네이버 링크를 쓰도록 안내한다.
- `route.py` — 자차 경로(OSRM 공개 서버) + 지도 링크→좌표 모듈. 표준 라이브러리만,
  **인증키 불필요**. 요청 간 1초 간격을 강제하고, 좌표쌍이 같으면 영구 캐시한다
  (집·유치원 위치는 바뀌지 않는다). `python route.py selftest`.
- `profile`·`compare`에 `집에서 자차(도로)` 행, `report` 비교표에도 동일 행 추가.
- **우회율 2배 이상이면 ⚠ 표시**하고, `report` 방문 질문지에 동선을 묻는 항목이
  자동 생성된다("직선거리보다 도로가 2.2배 돌아갑니다… 진입로가 일방통행인가요?").

### Notes

- **실시간 교통은 반영되지 않는다**(아침 등원 시간대는 더 걸림). 한국 도로망은
  OpenStreetMap 기반이라 일방통행·골목의 최신성이 상용 지도보다 떨어질 수 있다.
  두 한계를 출력에 항상 함께 표시한다. 조회 실패 시 값을 지어내지 않고 `-`로 둔다.
- `.env.example`과 문서의 좌표 설명을 `home` 명령 방식으로 다시 썼다.

## [1.6.0] - 2026-09-01

**시간 축** — 스냅샷에서 추세로. "지금 좋은 곳"과 "좋아지고 있는 곳"을 구분한다.

### Added

- **`trend`** — 후보 유치원의 충원율·만3세 원아·전체 원아/정원·교사 근속 1년 미만
  비율을 최근 5개 공시 차수(≈3년)로 보여준다. 유치원알리미 사이트의 시계열 화면은
  서비스 중단 상태라, 공시자료 일괄 다운로드(2013년~)를 읽어 추이를 복원했다.
- **`diff`** — 최신 두 차수 사이의 후보 변화만 추린다. 새 공시 감지 📢 알림과 이어져
  "게시 → refresh → diff" 흐름이 완성된다.
- **`pick`** — 후보 저장(`--show`/`--clear`). 저장하면 `report`/`trend`/`diff`를
  인자 없이 쓸 수 있다. 후보 목록은 `shortlist.json`에 저장되며 git에 올라가지 않는다.
- `kinderbulk.py` — 과거 차수 일괄 데이터 모듈(표준 라이브러리만, 키 불필요).
  **과거 차수는 불변이므로 영구 캐시**(`cache/bulk/`, `refresh`가 지우지 않음).
  헤더 이름 기반 파싱·검산·`python kinderbulk.py selftest` — kinderweb 과 같은 방어.
- `report`에 충원율 추이 행과 유치원별 추이 표 기본 포함(`--no-trend`로 생략).
- MCP 3종 추가(`save_shortlist`, `kindergarten_trend`, `kindergarten_diff`) — 총 11종.
  `kindergarten_report`의 지역·이름 인자는 비우면 저장된 후보를 쓴다.

### Notes

- 차수 간 유치원 매칭은 일괄 데이터에 코드가 없어 이름+주소로 한다. 폐원·신설로
  비는 차수는 `-`로 표시한다. 추세 화살표는 첫↔끝 차수 단순 비교이며 해석은 붙이지 않는다.

## [1.5.2] - 2026-09-01

### Added

- **`check_docs.py`** — 문서 동기화 점검 도구. MCP 도구 목록↔README,
  루트 파이썬 파일↔AGENTS 구조 표, `__version__`↔CHANGELOG, CLI 명령↔README·스킬을
  기계적으로 대조한다. v1.5.1에서 사람이 놓쳤던 종류의 어긋남을 릴리스 전에 잡기 위한 것.
  릴리스 절차 3번으로 편입(AGENTS.md).

## [1.5.1] - 2026-09-01

### Fixed

- 문서가 v1.5.0을 따라오지 못한 부분 정리.
  - README의 MCP 도구 목록이 6종으로 남아 있던 것을 8종으로 갱신
    (`kindergarten_schedule`, `kindergarten_report` 누락 보완).
  - AGENTS.md 저장소 구조 표에 `neis.py`·`kinderweb.py` 추가.
  - AGENTS.md 검증 절에 `kinderweb.py selftest`(웹 공시 파서 3종) 추가.

## [1.5.0] - 2026-09-01

후보를 좁힌 뒤의 마지막 단계 — **한 문서로 정리하고, 방문 가서 무엇을 물을지**까지.

### Added

- **`report` 명령** — 최종 후보 1~4곳의 비교표 + 유치원별 상세 + **방문 질문지**를
  한 문서로 만든다. `--out 파일.md`로 저장 가능.
  - 질문지는 공시 데이터의 이상 신호를 규칙으로 변환한다: 병설이면 방학 중 방과후
    인력(실측 방학 일수 인용), 대상 연령반 없음이면 모집 여부, 근속 1년 미만 30%↑이면
    교사 변동, 비월단위 납부 항목이면 연간 총액, 시정명령 이력이 있으면 경위(맨 앞) 등.
  - 원비·시정명령·**유치원 평가**(웹 공시)와 병설 방학 실측(NEIS)이 기본 포함.
- **유치원 평가 조회** — 평가 실시 이력(학년도별)과 평가결과 PDF 공시 여부를
  `profile --web`·`report`에 표시. 평가 내용 자체는 PDF라 원본 링크로 안내한다.
- **새 공시 감지** — 하루 1회, 가장 작은 지역 하나만 가볍게 확인해 새 공시 차수가
  게시되면 조회 결과 끝에 📢 알림을 붙인다. 실패해도 본 기능에 영향 없음.
- **`refresh` 명령** — 캐시를 비워 다음 조회부터 최신 공시를 받는다.
- **MCP 도구 패리티** — `kindergarten_schedule`(방학)과 `kindergarten_report`(브리핑)
  신설, `search_kindergartens`에 `near_km`, `compare_kindergartens`에 `age`/`target` 추가.
  이제 CLI의 모든 기능을 말로 쓸 수 있다.

### Changed

- `profile`·`compare` 내부를 공용 함수(`gather_sections`·`summary_items`·
  `compare_table_lines` 등)로 추출 — `report`와 공유하며 동작은 동일하다.

## [1.4.0] - 2026-09-01

Open API에 없는 **원비와 시정명령 이력**을 인증키 없이 조회할 수 있게 했다.

### Added

- **`profile --web`** — 유치원알리미 웹 공시 화면에서 두 가지를 함께 가져온다.
  - **시정명령·행정처분 이력**: 대부분 "이력 없음"이지만, 있다면 후보에서 빼야 할 정보
  - **원비**: 교육과정비·방과후비 합계(월, 연령별), 부담금 있는 항목, 특성화 활동
- `kinderweb.py` — 웹 공시 조회 모듈(표준 라이브러리만, 인증키 불필요).
  공식 API가 아니라 화면 개편에 대비해 네 겹으로 방어한다.
  - 표를 위치가 아니라 **이름(법정 공시 항목명)**으로 찾는다 — rowspan/colspan을
    펼친 격자 위에서 머리글·행 라벨로만 값을 집는다
  - 읽은 값을 검산하고, 어긋나면 **틀린 값 대신 명시적으로 실패**한다
  - 결과에 항상 **원본 페이지 링크**를 담아 사람이 검증할 수 있게 한다
  - `python kinderweb.py selftest` 로 화면 구조가 그대로인지 확인한다
- MCP `kindergarten_profile`에 `web` 매개변수 추가.
- README에 **키·설정 한눈표** 신설 — 필수는 유치원알리미 인증키 하나뿐이고
  나머지(NEIS·집 좌표·생년월)는 선택임을 명시. AGENTS.md에도 같은 지도를 실었다.

### Notes

- 웹 조회가 실패해도 `profile`의 나머지 항목은 그대로 출력된다.
- 공공누리 제1유형 자료이며 robots.txt가 수집을 전면 허용한다. 요청 사이에
  간격을 두어 서버에 부담을 주지 않는다.

## [1.3.0] - 2026-09-01

집에서의 거리로 후보를 추리고, **데이터로 알 수 없는 정차 여건은 눈으로 확인**하도록 이었다.

### Added

- **`--near KM`** — 집에서 직선 N km 이내만 검색. `--sort dist`로 가까운 순 정렬.
  `.env`의 `HOME_LATLNG=위도,경도`로 기준점을 잡는다. 유치원 좌표는 공시에 이미
  들어 있어(전 지역 100% 보유) 추가 API가 필요 없다.
- **로드뷰·지도 링크 자동 생성** — `profile`과 `compare`에 카카오 로드뷰 링크를 넣는다.
  자차 등하원에서 가장 중요한 **"유치원 앞에 잠시 정차할 수 있는가"**는 어떤 공시·API에도
  없다. 도구가 링크까지 만들어 주고 판단은 사람이 눈으로 한다.
- **통학차량 관점** — 차량을 운행하면 거리보다 **노선이 우리 동네를 지나는지**가
  중요해진다. 그 사실을 명시하고 전화 확인을 안내한다.

### Notes

- **직선거리는 언덕과 도로를 무시한 하한값이다.** 지형이 험한 지역에서는 실제 이동 편의와
  크게 다를 수 있어, 후보를 추리는 용도로만 쓰도록 출력에 명시했다.
- 도로명주소를 좌표로 자동 변환하는 신뢰할 만한 무료 방법이 없어(건물번호를 무시하고
  도로만 잡는다) 집 좌표는 지도 앱에서 직접 얻어 넣도록 했다.

## [1.2.0] - 2026-09-01

병설유치원의 **방학이 언제 며칠인지** 알 수 있게 했다.

### Added

- **`schedule` 명령** — 초등학교 병설유치원의 방학·학사일정을 보여준다.
  유치원알리미에는 학사일정이 없지만, 병설유치원은 모(母)초등학교에 붙어 있고
  초등학교 학사일정은 NEIS(교육정보 개방 포털)가 공개한다. 유치원명에서 `병설유치원`을
  떼어 초등학교를 찾고 주소로 검증한다(한 자치구의 병설 6곳으로 전수 확인).
- `neis.py` — NEIS 연동 모듈. 표준 라이브러리만 사용하며, 이 모듈이 실패해도
  나머지 기능은 영향을 받지 않는다.
- `--meals` 옵션 — 모초등학교 급식 식단(메뉴·칼로리)도 함께 표시.
- `NEIS_API_KEY` 설정 추가(선택). https://open.neis.go.kr 에서 무료 발급.
  **없어도 다른 기능은 모두 정상 동작한다.**

### Notes

- **조회 결과는 초등학교 일정이다.** 병설유치원은 대체로 이를 따르지만 정확히 같지는
  않다(유치원 법정 수업일수 180일, 초등학교 190일). 출력에 이 한계를 명시한다.
- **사립·단설 유치원은 NEIS에 없다.** 해당 유치원을 조회하면 그 사실과 함께
  공시 수업일수·유치원 전화번호를 안내한다.

## [1.1.0] - 2026-09-01

"공시된 운영시간이 실제로 아이가 다닐 수 있는 시간인가"를 확인할 수 있게 했다.

### Fixed

- **방과후과정 참여율이 일부 유치원에서 0%로 잘못 계산되던 문제.**
  공시 양식이 두 가지(인가 학급 그대로 편성 `inor_*` / 오후 재편성 `pm_rrgn_*`)인데
  앞의 것만 세고 있었다. 후자로 공시한 유치원은 실제 참여율이 100%인데도 0%로 표시됐다.
- 해당 연령반이 없는 유치원(혼합반만 운영)의 수업일수가 `0일`로 표시되던 문제. 이제 `-`로 표시한다.

### Added

- **등원 가능일수** — 정규 수업일수와 방과후 포함 운영일수를 함께 보여준다.
  병설유치원은 초등학교 학사일정을 따라 정규 수업일수가 법정 최소(180일)에 가깝고
  사립은 220~240일이라 **50일가량 차이 난다.** 다만 방학 중에도 방과후과정이 운영되어
  '방과후 포함 운영일수'는 양쪽이 비슷하다. 즉 병설은 다닐 날이 적다기보다
  **방학 기간을 방과후과정에 의존**하는 구조다.
- **운영시간 해석 안내** — 공시된 운영시간은 '문 여는 시간'이지 정규 교육과정 시간이 아니다.
  정규 교육과정은 법정 1일 4~5시간이고 나머지는 방과후·돌봄이다(병설·사립 공통).
- `compare`에 `방과후 포함 운영일수`, `방과후 의존일수(방학 등)` 행 추가.
- 스킬에 병설/사립 판단 기준과 방과후 전담인력 확인 지침 추가.

## [1.0.0] - 2026-09-01

최초 공개 릴리스.

### Added

- 유치원알리미 Open API 14개 공시 항목을 조회하는 CLI (`kinderinfo.py`).
  파이썬 표준 라이브러리만 사용합니다.
  - `search` — 지역별 유치원 검색. 연령반·설립유형·이름 필터, 충원율/정원 정렬
  - `profile` — 유치원 1곳의 공시 항목 종합 리포트와 파생 지표
  - `compare` — 여러 유치원의 핵심 지표 비교표
  - `raw` — 엔드포인트 원본 JSON
  - `discover` — 시군구 코드 자동 탐색. API가 코드표를 제공하지 않아 직접 훑습니다
  - `regions` — 저장된 지역 코드 목록
- 파생 지표: 정원 충원율, 연령별 학급당 원아 수, 교사 근속 추정 평균과 분포,
  원아 1인당 교실면적, CCTV 실내외 대수, 통학차량, 방과후과정 참여율
- `--target` 옵션. `.env`의 `CHILD_BIRTH_YM`으로 입학 학년도와 연령반을 자동 계산합니다
  (만3세반 입학 학년도 = 출생연도 + 4).
- `--age 3|4|5` 옵션으로 대상 연령반 지정.
- MCP 서버 (`mcp_server.py`). Claude Code와 Codex 양쪽에서 같은 파일로 동작합니다.
  도구 6종: `search_kindergartens`, `kindergarten_profile`, `compare_kindergartens`,
  `raw_data`, `list_regions`, `discover_region`.
- Claude Code / Codex 공용 스킬과 배포 스크립트 (`sync_skill.py`).
- 초보자 설치·사용을 단계별로 안내하는 에이전트 문서 (`AGENTS.md`).
- API 응답 디스크 캐시 (기본 7일). `--fresh`로 우회합니다.

### Security

- API 인증키를 `.env`로 분리했습니다. 우선순위는 환경변수 → `.env` → `config.json`(구버전 호환)이며,
  `.env`·`config.json`·`cache/`·`sgg_codes.json`은 `.gitignore`로 제외됩니다.

### Fixed

- 인증키가 잘못된 경우 API가 `DENIED`를 반환하는데, 이를 "공시 항목 점검 중"으로
  잘못 안내하던 문제. 이제 키 문제와 항목 점검을 구분해 각각의 조치를 안내합니다.

[1.0.0]: https://github.com/kaicot/kaic-kinder-info/releases/tag/v1.0.0
