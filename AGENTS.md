# AGENTS.md

이 문서는 사람이 아니라 **AI 코딩 에이전트**(Codex, Claude Code 등)가 읽는 안내서입니다.
사용자가 이 저장소를 가리키며 *"설치해줘"*, *"이걸로 유치원 찾아줘"* 라고 하면 아래 절차를 따르세요.

## 이 도구가 하는 일

교육부 **유치원알리미**(e-childschoolinfo.moe.go.kr) Open API를 감싼 명령줄 도구입니다.
전국 유치원을 지역·연령반으로 검색하고, 한 곳의 공시 항목 14종을 종합 리포트로 뽑고,
여러 곳을 표로 비교합니다. 파이썬 표준 라이브러리만 쓰므로 설치할 패키지가 없습니다.

사용자가 직접 준비해야 하는 것은 **유치원알리미 Open API 인증키 하나뿐**입니다.

---

## 사용자 응대 원칙 — 먼저 읽으세요

이 도구를 쓰려는 사람은 **프로그래밍을 전혀 모르는 학부모일 가능성이 높습니다.**
`.env`가 뭔지, 터미널이 뭔지 모른다고 가정하고 대하세요.

- **명령어는 당신이 대신 실행하세요.** 사용자에게 "터미널을 열고 이걸 입력하세요"라고
  떠넘기지 마세요. 당신에게 실행 도구가 있다면 직접 실행하고 결과만 설명하면 됩니다.
- **한 번에 한 단계씩** 진행하고, 각 단계가 성공했는지 확인한 뒤 다음으로 넘어가세요.
- **전문용어를 쓸 때는 풀어서 설명하세요.** ".env 파일에 환경변수를 설정합니다"가 아니라
  "발급받은 키를 설정 파일에 적어둘게요"라고 말하세요.
- **에러가 나면 추측하지 말고** 에러 메시지 원문을 먼저 확인하세요. 아래 [문제 해결](#문제-해결)에
  실제 증상과 원인이 정리돼 있습니다.
- **인증키는 비밀번호와 같습니다.** 화면에 전체를 반복해 출력하거나, 커밋하거나,
  외부로 보내지 마세요. `.env`는 이미 `.gitignore`에 등록돼 있습니다.
- 사용자가 키를 채팅창에 붙여넣기 꺼려하면, `.env` 파일을 직접 편집하는 방법을 안내하세요.

---

## 시나리오 A — "이거 설치해줘"

### 0단계. 이미 설치돼 있는지 확인

작업 폴더에 `kinderinfo.py`가 있고 `.env`도 있다면 이미 설치된 상태입니다.
`python kinderinfo.py --version`으로 확인하고, 정상이면 [시나리오 B](#시나리오-b--이걸로-유치원-찾아줘)로 넘어가세요.

### 1단계. 저장소 내려받기

사용자가 원하는 위치에 내려받으세요. 특별한 요청이 없으면 홈 디렉터리 아래
`kaic-kinder-info` 정도가 무난합니다.

```bash
git clone https://github.com/kaicot/kaic-kinder-info.git
cd kaic-kinder-info
```

`git`이 없다면 GitHub 웹페이지의 **Code → Download ZIP**으로 받아 압축을 풀게 안내하세요.

### 2단계. 파이썬 확인

**파이썬 3.9 이상**이 필요합니다. 어떤 명령이 동작하는지 먼저 찾으세요.

```bash
python --version     # Windows에서 보통 이것
python3 --version    # Mac/Linux에서 보통 이것
py --version         # Windows 대안
```

- 셋 다 안 되면 파이썬이 없는 것입니다. https://www.python.org/downloads/ 에서 설치하도록
  안내하세요. **Windows 설치 시 "Add python.exe to PATH" 체크박스를 반드시 켜야 합니다.**
- Windows에서 `python`을 쳤는데 **Microsoft Store가 열린다면** 가짜 파이썬(스텁)입니다.
  python.org 버전을 설치하거나 `py`를 쓰세요.
- 이후 단계에서는 여기서 확인된 명령을 일관되게 쓰세요.

### 3단계. 인증키 발급 — ★ 사용자가 직접 해야 하는 유일한 단계 ★

**여기서 반드시 멈추고 사용자를 기다리세요.** 당신이 대신 발급받을 수 없습니다.

사용자에게 이렇게 안내하세요.

1. https://e-childschoolinfo.moe.go.kr/openApi/openApiIntro.do 접속
   (메뉴로는 **자료실 → OPEN API**)
2. 사이트 회원가입/로그인이 필요할 수 있습니다.
3. Open API 사용 신청서를 작성합니다. 서비스 목록에서
   **"유치원 기본현황 정보 조회"** 를 선택하면 됩니다.
4. 심의가 **자동승인**이면 바로 인증키가 나옵니다. 아니면 승인까지 기다려야 합니다.
5. 발급된 인증키는 **영문+숫자 32자리** 형태입니다. 이걸 복사해 오라고 하세요.

사용자가 키를 가져오면 4단계로 갑니다. 키를 채팅에 붙여넣기 부담스러워하면,
`.env.example`을 `.env`로 복사해 두고 **사용자가 직접 그 파일을 열어 키를 적게** 안내하세요.

### 4단계. `.env` 만들기

`.env.example`을 복사해 `.env`를 만들고 키를 채워 넣으세요.

```bash
cp .env.example .env      # Windows: copy .env.example .env
```

`.env` 안의 이 줄을 수정합니다.

```
KINDER_API_KEY=여기에_발급받은_인증키를_붙여넣으세요
```

**따옴표 없이, 앞뒤 공백 없이** 키만 붙여 넣어야 합니다. 이게 가장 흔한 실수입니다.

### 5단계. 동작 확인

인증키가 실제로 먹히는지 확인합니다. 서울 강남구(코드 11680)를 한 번 조회하는
가장 빠른 검사입니다. `--fresh`를 붙여야 캐시를 건너뛰고 진짜로 서버에 물어봅니다.

```bash
python kinderinfo.py search 11680 --limit 3 --fresh
```

유치원 목록이 표로 나오면 성공입니다. 실패하면 [문제 해결](#문제-해결)을 보세요.

### 6단계. 사용자 지역 준비

이 API는 시군구 코드를 요구하는데, 코드표가 공개돼 있지 않아 도구가 직접 훑어서 찾아냅니다.
사용자가 사는 **시도**를 물어보고 한 번 실행하세요. 1~2분 걸립니다.

```bash
python kinderinfo.py discover 서울
```

- 결과는 `sgg_codes.json`에 저장되며, 그 지역은 다시 탐색하지 않습니다.
- **경기도는 `--full`을 붙여야 합니다**(`수원시 장안구`처럼 시 안에 구가 있는 구조).
  5~10분 걸리니 사용자에게 미리 알리세요.
- 사실 이 단계를 건너뛰어도 됩니다. `search 서울` 처럼 시도 이름을 쓰면 자동으로 탐색이 돕니다.

### 7단계. (선택) 아이 생년월 설정

`.env`에 아이 생년월을 적어두면 `--target` 옵션이 입학 학년도와 연령반을 자동 계산합니다.

```
CHILD_BIRTH_YM=2020-03
```

이후 `python kinderinfo.py search "서울 강남구" --target` 처럼 쓰면
"2020년 3월생 → 2024학년도 만3세반 대상" 같은 안내와 함께 해당 연령반만 걸러줍니다.
(계산식: 만3세반 입학 학년도 = 출생연도 + 4)

### 7-2단계. (선택) NEIS 인증키 — 병설유치원 방학 조회용

사용자가 **초등학교 병설유치원**을 후보에 두고 있다면 권하세요. 병설은 초등학교 학사일정을
따라 방학이 길고(실측 67~85일), 그 일정이 유치원알리미에는 없습니다.

1. https://open.neis.go.kr 에서 회원가입 후 인증키를 신청합니다(무료).
2. `.env`에 `NEIS_API_KEY=발급받은키` 를 추가합니다.
3. 확인: `python kinderinfo.py schedule "<지역>" <병설유치원명>`

**없어도 다른 기능은 모두 정상 동작합니다.** 사립·단설만 볼 거라면 건너뛰어도 됩니다.

### 8단계. (선택) MCP 등록 — 매번 폴더를 찾지 않고 쓰기

MCP로 등록해 두면 사용자가 아무 대화창에서나 "강남구 유치원 알려줘"라고 말만 해도
당신이 이 도구를 바로 호출할 수 있습니다. **파이썬 절대 경로가 필요합니다.**

```bash
# 절대 경로 찾기
where python          # Windows
which python3         # Mac/Linux
```

```bash
# Claude Code
claude mcp add --scope user kaic-kinder-info -- "<파이썬 절대경로>" "<저장소 절대경로>/mcp_server.py"

# Codex
codex mcp add kaic-kinder-info -- "<파이썬 절대경로>" "<저장소 절대경로>/mcp_server.py"
```

- **`python`이라고만 쓰면 안 됩니다.** Windows에서는 Microsoft Store 스텁으로 잡혀
  `CONNECTION_CLOSED`로 실패합니다. 반드시 절대 경로를 쓰세요.
- MCP 서버는 `mcp` 파이썬 패키지를 필요로 합니다. 없으면 `pip install mcp`로 설치하세요.
  (CLI만 쓸 거면 필요 없습니다.)
- 등록 후 `claude mcp list` 또는 `codex mcp get kaic-kinder-info`로 연결을 확인하세요.

### 9단계. (선택) 스킬 배포

Claude Code / Codex가 이 도구의 사용법과 데이터 해석 기준을 항상 알고 있게 하려면:

```bash
python sync_skill.py
```

`~/.claude/skills/`와 `~/.codex/skills/` 양쪽에 스킬이 복사됩니다.

### 설치를 마치면

사용자에게 이렇게 정리해 주세요. 명령어를 외우게 하지 말고, **말로 물어보면 된다**는 걸
알려주는 게 핵심입니다.

- 이제 "강남구에서 만3세반 있는 유치원 찾아줘" 처럼 말로 물어보시면 됩니다.
- 공시 자료는 1년에 두 번(4월 말, 10월 말경) 갱신됩니다. 원서 접수 직전에는
  `--fresh`를 붙여 최신 자료를 다시 받는 게 좋습니다.
- 원비·모집요강·경쟁률은 이 자료에 없습니다. **처음학교로**(www.go-firstschool.go.kr)나
  유치원에 직접 문의해야 합니다.

---

## 시나리오 B — "이걸로 유치원 찾아줘"

이미 설치돼 있다면 바로 쓰면 됩니다. 사용자의 질문을 아래 명령으로 옮기세요.

| 사용자가 이렇게 말하면 | 이렇게 실행 |
|---|---|
| "강남구 유치원 목록 보여줘" | `search "서울 강남구"` |
| "만3세반 있는 곳만" | `search "서울 강남구" --age 3` |
| "우리 아이 나이에 맞는 곳" | `search "서울 강남구" --target` |
| "공립만 보여줘" | `search "서울 강남구" --age 3 --estab 공립` |
| "○○유치원 자세히" | `profile "서울 강남구" ○○` |
| "A랑 B 비교해줘" | `compare "서울 강남구" "A,B"` |
| "거기 방학 언제야?" | `schedule "서울 강남구" ○○초등학교병설` (병설만) |
| "정원 많은 순으로" | `search ... --sort size` |

**출력을 그대로 붙여넣지 말고 요약해서 답하세요.** 사용자가 실제로 궁금한 건
"어디가 괜찮은가"이지 표 전체가 아닙니다. 아래 해석 기준을 활용하세요.

- **충원율**(원아수/정원): 높으면 인기가 많다는 신호, 지나치게 낮으면(30% 미만) 이유를 확인할 만합니다.
- **교사 근속**: "1년 미만 비율"이 높으면 교사 이직이 잦다는 뜻입니다. 평균값은
  공시 구간의 중간값으로 계산한 **추정치**이므로 단정적으로 말하지 마세요.
- **학급당 원아 수**: 적을수록 아이 한 명당 돌봄이 여유롭습니다. 공립 병설이 보통 더 적습니다.
- **혼합연령 학급**: 연령 필터에 걸리지 않아도 혼합반에 해당 연령이 포함될 수 있습니다.
  후보를 좁혔으면 `profile`로 한 번 더 확인하세요.
- **없는 정보는 지어내지 마세요.** 원비, 모집요강, 경쟁률, 통학구역은 이 API에 없습니다.

---

## 문제 해결

| 증상 | 원인 | 조치 |
|---|---|---|
| `[오류] API 인증키가 없습니다` | `.env`가 없거나 키가 안 적힘 | 4단계 재확인 |
| `[오류] 인증키가 거부되었습니다 (유효하지 않은 키)` | 키 오타, 따옴표·공백 포함 | `.env`의 키를 다시 붙여넣기 |
| `[안내] 공시 항목이 현재 점검 중입니다` | 교육부가 해당 항목을 잠근 상태 | 도구 문제가 아님. 나중에 재시도 (2026-09 기준 교직원현황·급식운영현황이 점검 중) |
| 검색 결과가 0곳 | 지역명 오타 또는 코드 미탐색 | `regions`로 저장된 지역 확인, `discover <시도>` 실행 |
| `지역을 해석할 수 없습니다` | 시도 없이 흔한 구 이름만 입력 | `"서울 강남구"`처럼 시도를 붙여서 재시도 |
| 키를 고쳤는데도 옛 결과가 나옴 | 7일짜리 캐시가 응답 중 | `--fresh` 추가 |
| `python`이 Microsoft Store를 엶 | Windows 가짜 파이썬 스텁 | python.org 버전 설치, 또는 `py` 사용 |
| MCP가 `CONNECTION_CLOSED` | `python`을 상대 경로로 등록함 | 파이썬 **절대 경로**로 다시 등록 |
| MCP 등록 후 `ModuleNotFoundError: mcp` | `mcp` 패키지 미설치 | `pip install mcp` |
| 한글이 깨져 보임 | 터미널 인코딩 | 도구는 UTF-8로 출력함. 터미널 설정 확인 |

진단이 막히면 `--json`을 붙여 원자료를 보거나, `raw basicInfo2 <지역>`으로
API 원본 응답을 직접 확인하세요.

---

## 이 저장소를 수정할 때

### 구조

| 파일 | 역할 |
|---|---|
| `kinderinfo.py` | 핵심 CLI. 모든 로직이 여기 있음 |
| `mcp_server.py` | CLI를 MCP 도구로 노출 |
| `sync_skill.py` | 스킬 원본을 Claude/Codex 폴더에 배포 |
| `skill/kaic-kinder-info/SKILL.md` | 스킬 **원본**. 반드시 여기만 고칠 것 |

### 개발 규약

- **외부 의존성을 추가하지 마세요.** `kinderinfo.py`는 파이썬 표준 라이브러리만 씁니다.
  이게 "설치할 게 없다"는 이 도구의 핵심 장점입니다. (`mcp_server.py`만 예외적으로 `mcp` 사용)
- 출력은 **한국어 마크다운**입니다. 표는 그대로 인용 가능한 형태를 유지하세요.
- 새 공시 항목을 추가하면 `ENDPOINTS`와 `FIELD_LABELS`에 한국어 라벨을 함께 넣으세요.
  라벨 없는 필드는 원본 영문 키가 그대로 노출됩니다.
- 개인정보를 저장소에 넣지 마세요. 예시는 특정 지역에 쏠리지 않게 여러 지역을 섞어 쓰고,
  실제 아이 생년월 같은 값은 `.env`에만 둡니다.

### 검증

변경 후 최소한 이 세 개는 돌려보세요.

```bash
python kinderinfo.py --version
python kinderinfo.py search 11680 --limit 3 --fresh    # API 왕복 확인
python kinderinfo.py profile 11680 강남유정             # 14개 항목 파싱 확인
```

### 릴리스 절차 (유의적 버전)

1. `kinderinfo.py`의 `__version__` 수정
   - **MAJOR**: CLI 옵션이나 MCP 도구 이름이 바뀌어 기존 사용법이 깨질 때
   - **MINOR**: 기능 추가 (하위 호환 유지)
   - **PATCH**: 버그 수정, 문서 정리
2. `CHANGELOG.md`에 항목 추가 (Added / Changed / Fixed / Removed)
3. 커밋 후 태그: `git tag -a v1.1.0 -m "..."` → `git push --tags`

### API 함정 (재발견 비용이 큰 것들)

- **`sggCode`는 필수입니다.** `sidoCode`만 넣으면 빈 배열이 옵니다. 그래서 `discover`가 존재합니다.
- **`DENIED`는 두 가지 뜻**입니다 — 인증키 문제이거나, 항목이 점검 중이거나.
  `fetch()`에서 메시지에 "키"가 있는지로 구분합니다.
- **숫자에 단위가 붙어 옵니다**(`781㎡`, `12개`). 반드시 `to_int()`로 파싱하세요.
- **MCP 서버 안에서 `subprocess`를 쓰지 마세요.** Windows에서 교착됩니다.
  `mcp_server.py`는 CLI를 같은 프로세스에서 호출하고 stdout을 가로채는 방식입니다.
- 공시 차수(`pbnttmng`) `20261`은 "2026년 1차"라는 뜻입니다. 연 2회 갱신됩니다.
